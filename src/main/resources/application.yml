server:
  port: 8888

spring:
  application:
    name: config-server

  profiles:
    active: git   # IMPORTANT

  cloud:
    config:
      server:
        git:
          uri: https://github.com/arelliprashanthi31-del/config-repo